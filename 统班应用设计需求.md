# 统班应用设计需求（反向推导版）

> 本文档基于当前工程文件反向整理，阅读范围包括 `README.md`、`main.py`、`src/` 源码、`tests/` 自动化测试与手工测试清单、`data/config.json`、`tests/sample_files/` 样例文件、`output/` 已生成报表，以及曾用于解析版式的 `2026春季学期5-8周助理值班统计.docx` 成品样本。
>
> 文档描述的是“当前项目实际体现出来的需求”。其中仍无法仅凭项目确认的部分，已统一放在末尾“待确认问题”。

## 1. 项目定位

统班应用（TonBanAPP）是面向“资助服务中心”值班管理场景的桌面工具，用于帮助管理员从 Word 名单/排班文件中提取人员和值班信息，生成周统计 Excel 和月度 Word 统计报表。

应用的核心目标是减少人工统计班次、核减假期、结转上月多值/缺班、月度 Word 排版等重复工作。

## 2. 目标用户与使用场景

### 2.1 目标用户

- 资助服务中心排班或值班统计管理员。
- 需要定期维护助理总名单、每周统计值班情况、每月汇总出正式 Word 报表的工作人员。

### 2.2 典型场景

1. 管理员导入一份本学期/本阶段助理总名单 Word。
2. 管理员按周选择“排班名单 Word”和“实际周统计名单 Word”，勾选本周放假日期，生成周统计 Excel。
3. 管理员每月选择若干份周统计 Excel，必要时选择上月月度 Word 作为结转依据，生成本月月度 Word 报表。
4. 管理员可在基础设置中维护“大四助理”名单，并通过总开关控制是否把这些人的每周应值班次固定为 1 次。
5. 管理员可在月统计阶段补录额外加班班次，使其计入月度“总计班次”。

## 3. 技术与运行环境需求

- 应用形态：Python 桌面应用。
- GUI 框架：`customtkinter` + `tkinter`/`ttk`。
- Word 处理：`python-docx`。
- Excel 处理：`pandas` + `openpyxl`。
- 中文姓名排序：`pypinyin`。
- 测试：`pytest`、`pytest-cov`。
- 支持 Python 3.8+；当前本地虚拟环境为 Python 3.14.3。
- 程序入口为 `main.py`，启动命令为：

```bash
python main.py
```

## 4. 页面与导航需求

应用主窗口标题为“资助服务中心 - 统班应用”，默认尺寸约 `980x750`。

主界面采用三个标签页：

1. 基础设置
2. 周统计生成
3. 月统计Word生成

## 5. 基础设置需求

### 5.1 总名单导入

用户应能从 Word `.docx` 文件导入或更新总名单。

导入规则：

- 读取 Word 正文段落和所有表格单元格文本。
- 从文本中提取 2 到 4 个连续中文字符作为姓名。
- 支持姓名内部全角空格，例如“张　爽”应被识别为“张爽”。
- 普通空格用于分隔姓名，不应把“张三 李四”错误拼成一个姓名。
- 导入结果需要去重，并保持首次出现顺序。
- 导入成功后更新本地配置中的：
  - `name_list`
  - `total_count`
- 如果 Word 没有解析到姓名，应提示用户检查文档内容，程序不应崩溃。

### 5.2 总名单展示

基础设置页应展示当前总名单人数。

用户可点击“查看当前名单”刷新展示区。

名单展示当前按多列文本排布：

- 总名单按 6 列展示。
- 大四助理确认名单按 5 列展示。

### 5.3 本地持久化

总名单和基础设置应持久化到 `data/config.json`。

配置文件不存在时应自动使用空结构：

```json
{
  "name_list": [],
  "total_count": 0,
  "senior_assistants": [],
  "senior_should_fixed_enabled": false
}
```

配置文件损坏或无法解析时，应用应回退为空配置，不应崩溃。

### 5.4 大四助理规则配置

基础设置页需要提供“大四助理规则（周统计）”配置区。

配置能力：

- 从当前总名单中多选大四助理。
- 保存所选大四助理名单。
- 提供总开关“大四助理规则”，控制规则是否在周统计中生效。
- 显示当前开关状态和已选择人数。
- 显示已确定的大四助理名单。

数据约束：

- 大四助理只能从当前总名单中选择。
- 保存时应过滤不在总名单中的姓名。
- 保存时应去重并保持选择顺序。
- 更新总名单时，已不存在于新总名单中的大四助理应自动移除。
- 开关状态和大四助理名单均需持久化。

当前 `data/config.json` 中已有：

- 总名单人数：84 人。
- 大四助理人数：20 人。
- 大四规则开关：开启。

## 6. Word 解析需求

### 6.1 总名单 Word 解析

总名单 Word 可由段落或表格组成。

解析器应遍历：

- `doc.paragraphs`
- `doc.tables[*].rows[*].cells[*].paragraphs`

姓名识别采用中文姓名正则，当前规则为：

```text
[\u4e00-\u9fa5]{2,4}
```

### 6.2 周排班/实际名单 Word 解析

周统计 Word 解析结果应为按星期归类的姓名列表：

```python
{
  "周一": ["张三", "李四"],
  "周二": [],
  ...
}
```

支持识别的星期标题别名：

- 周一：`周一`、`星期一`、`礼拜一`、`周1`
- 周二：`周二`、`星期二`、`礼拜二`、`周2`
- 周三：`周三`、`星期三`、`礼拜三`、`周3`
- 周四：`周四`、`星期四`、`礼拜四`、`周4`
- 周五：`周五`、`星期五`、`礼拜五`、`周5`
- 周六：`周六`、`星期六`、`礼拜六`、`周6`
- 周日：`周日`、`星期日`、`星期天`、`礼拜日`、`礼拜天`、`周7`

解析规则：

- 按文档出现顺序处理文本行。
- 遇到单独成行的星期标题后，该标题到下一个星期标题之间的人名归入当前星期。
- 星期标题必须单独成行，例如一行只写“周一”或“星期一”；“周一 张三 李四”不再作为有效星期标题解析。
- 只保留当前总名单中的姓名。
- 重复出现的姓名要保留，用于表示同一人多次排班/值班。
- 课节号等非姓名内容应被过滤。
- 支持段落和表格单元格。

## 7. 周统计 Excel 需求

### 7.1 输入

生成周统计前必须满足：

- 已导入总名单。
- 用户选择有效的“排班名单 Word”。
- 用户选择有效的“实际周统计名单 Word”。
- 用户选择输出保存路径，默认保存目录为 `output/`，默认文件名格式类似：

```text
周统计_YYYYMMDD_HHMMSS.xlsx
```

### 7.2 双 Word 统计口径

当前周统计采用双 Word 口径：

- 排班名单 Word：用于计算“应值班次”。
- 实际周统计名单 Word：用于计算“实际班次”。

如果底层函数未传实际 Word，则可回退使用排班 Word 兼容旧流程；但当前 UI 要求两个 Word 都选择。

### 7.3 应值班次计算

默认应值班次来自排班 Word 中该人员出现次数。

示例：

- 张三在排班 Word 中出现 2 次，则张三应值班次为 2。
- 李四未在排班 Word 中出现，则李四应值班次为 0。

该口径已确认：周统计“应值班次”以“排班 Word 中出现次数”为准，不再默认每人每周应值 2 次。

### 7.4 实际班次计算

实际班次来自实际周统计名单 Word 中该人员出现次数。

示例：

- 张三在实际 Word 中出现 3 次，则张三实际班次为 3。
- 未出现则实际班次为 0。

### 7.5 放假核减

用户可勾选本周放假的日期，当前 UI 提供：

- 周一
- 周二
- 周三
- 周四
- 周五

放假核减规则：

- 只对排班 Word 中该放假日期排到班的人生效。
- 核减数量等于该人在放假当天的排班出现次数。
- 应值班次核减后最低为 0。

示例：

- 张三周三排 1 次，周三放假，则张三应值班次减 1。
- 张三周三排 3 次，周三放假，则张三应值班次减 3。

### 7.6 大四助理应值固定规则

当“大四助理规则”开关开启，且人员在大四助理名单中时：

- 该人员本周应值班次固定为 1 次。
- 如果同时存在放假核减，则放假核减优先级更高。
- 实际计算表现为：先固定为 1，再扣减该人在放假日的排班次数，最低为 0。

示例：

- 张三是大四助理，原排班 2 次，无放假，则应值班次为 1。
- 王五是大四助理，原排班 0 次，无放假，也会被固定为 1。
- 张三是大四助理，周三排 1 次且周三放假，则应值班次为 `max(0, 1 - 1) = 0`。

当开关关闭时，大四助理名单不影响应值班次。

该口径已确认：大四助理固定 1 次只需要总开关控制，不需要配置起止日期、周次范围或其他特定时间段。

### 7.7 周统计加班

该口径已确认：周统计阶段不提供/计算加班补录。

实际班次仅来自实际周统计名单 Word。

### 7.8 缺班计算

缺班按以下规则计算：

```text
缺班 = max(0, 应值班次 - 实际班次)
```

因此实际班次多于应值班次时，缺班显示为 0，不显示负数。

### 7.9 输出 Excel

周统计 Excel sheet 名称为：

```text
周统计
```

输出列固定为：

1. 姓名
2. 应值班次
3. 实际班次
4. 缺班
5. 备注

排序规则：

- 按“缺班”降序排序。
- 缺班相同的人员保持总名单原有顺序。

样式要求：

- 首行加粗。
- 首行填充浅色背景。
- 全表居中。
- 全表添加细边框。
- 列宽按内容自适应。

异常处理：

- 输出文件被 Excel 占用时，应提示用户先关闭文件。
- 输入文件不存在或未选择时，应弹出中文提示，不应崩溃。

## 8. 月统计 Word 需求

### 8.1 输入

生成月统计 Word 前必须满足：

- 已导入总名单。
- 至少选择 1 个周统计 Excel。
- 可选择上月月度 Word；首次使用或无上月数据时可留空。
- 可配置月统计标题信息。
- 可选填月统计加班补录。
- 用户选择输出保存路径，默认保存目录为 `output/`，默认文件名格式类似：

```text
月度统计_YYYYMMDD_HHMMSS.docx
```

### 8.2 周 Excel 数量

界面提供 4 个周 Excel 输入位，对应一个月常见 4 周汇总。

但当前代码允许只选择 1 到 4 个周 Excel，至少 1 个即可生成。

### 8.3 周 Excel 格式要求

每个周统计 Excel 必须至少包含以下列：

- 姓名
- 应值班次
- 实际班次

聚合时只统计当前总名单中的人员。

缺少必要列时应抛出/提示解析失败。

### 8.4 月度标题

用户可手动配置：

- 年份：4 位数字，例如 `2026`
- 学期：`春季` 或 `秋季`
- 起始周：正整数
- 结束周：正整数，且不能小于起始周

标题格式强制拼装为：

```text
{年份}{春季/秋季}学期{起始周}-{结束周}周助理值班统计
```

示例：

```text
2026春季学期1-4周助理值班统计
```

默认学期规则：

- 当前月份为 2 到 7 月时，默认为春季。
- 其他月份默认为秋季。

### 8.5 月统计加班补录

月统计页提供“月统计加班补录”。

用户可：

- 从总名单下拉选择人员。
- 输入正整数班次。
- 点击添加。
- 点击清空。

计算规则：

- 补录只作用于月统计 Word。
- 补录班次叠加到该人员的“总计班次”。
- 同一人多次添加时，班次累加。
- 非正整数应提示错误。
- 不在总名单中的姓名不能补录。

### 8.6 上月 Word 结转

月统计支持选择上月月度 Word 作为结转来源。

结转规则：

```text
总计班次 = 本月周 Excel 的实际班次合计 + 上月多值班次 + 本月加班补录
应值班次 = 本月周 Excel 的应值班次合计 + 上月缺班数量
多值班次 = 总计班次 - 应值班次
缺班数量 = 应值班次 - 总计班次
```

首次生成或不选择上月 Word 时：

- 上月多值班次视为 0。
- 上月缺班数量视为 0。

### 8.7 人员流动

月统计以当前总名单为唯一人员基准。

规则：

- 上月 Word 中存在但当前总名单不存在的人，视为已离开，不进入本月结果，历史数据丢弃。
- 当前总名单新增但上月 Word 中不存在的人，视为新进人员，上月多值/缺班均按 0 计算。

### 8.8 上月 Word 反向解析

上月 Word 中必须存在一个表格，其表头至少包含：

- 姓名
- 多值班次
- 缺班数量

解析规则：

- 找到首个包含上述三列的表格。
- 表头之后每行读取姓名、多值班次、缺班数量。
- 姓名必须匹配 2 到 4 个中文字符。
- 空单元格按 0 处理。
- 非数字文本按 0 处理。
- 含左上到右下斜线（`w:tl2br`）的单元格按 0 处理。

如果找不到符合规范的表格，应提示上月 Word 表头不符合规范。

### 8.9 输出 Word 版式

月统计 Word 生成不再依赖外部模板文件。当前实现已将已生成成品 `2026春季学期5-8周助理值班统计.docx` 的关键版式解析并固化在代码中。

内置版式约定：

- 第 1 个表格为目标表格。
- 第 0 行为标题行。
- 第 1 行为表头行。
- 表格列顺序固定为：
  1. 序号
  2. 姓名
  3. 总计班次
  4. 应值班次
  5. 多值班次
  6. 缺班数量

生成要求：

- 从空白 Word 文档创建目标表格，不读取任何外部模板文件。
- 按 5-8 周成品文件内置页面尺寸、页边距、表格宽度、列宽、行高、字体字号、对齐、边框等样式。
- 替换标题。
- 序号列使用 Word 自动编号，从 1 开始。
- 自动编号定义必须由代码写入 Word 包中，不依赖外部模板已有的编号定义。
- 当“多值班次”或“缺班数量”小于等于 0 时：
  - 单元格文本置空。
  - 注入左上到右下斜线。
- 当“多值班次”或“缺班数量”大于 0 时：
  - 显示对应数字。
  - 不应保留斜线。

异常处理：

- 输出 Word 被占用时提示用户先关闭文件。
- 上月 Word 格式错误时提示解析失败，不应崩溃。

### 8.10 月统计排序

月统计 Word 排序规则：

1. 多值班次降序。
2. 缺班数量升序。
3. 姓名拼音升序。

## 9. 月统计 Excel 兼容需求

项目中仍保留 `generate_monthly_excel` 作为回退/兼容入口，但 UI 当前不暴露该功能。

月统计 Excel 输出列为：

1. 序号
2. 姓名
3. 总计班次
4. 应值班次
5. 多值班次
6. 缺班数量

样式与排序规则：

- 排序：多值班次降序、缺班数量升序、姓名拼音升序。
- 多值班次/缺班数量小于等于 0 的单元格清空并画斜线。
- 全表居中、加边框、首行加粗、列宽自适应。

## 10. 数据文件需求

### 10.1 `data/config.json`

保存应用基础配置：

```json
{
  "name_list": ["姓名1", "姓名2"],
  "total_count": 2,
  "senior_assistants": ["姓名1"],
  "senior_should_fixed_enabled": true
}
```

### 10.2 `output/`

默认保存生成报表。

该目录用于保存运行时生成的周统计 Excel 与月统计 Word，不建议纳入正式发布包。

### 10.3 `tests/sample_files/`

保存测试样例文件，包括：

- 总名单 Word 样例。
- 周排班 Word 样例。
- 月度 Word 版式样例。

## 11. 异常与提示需求

应用应对以下情况给出中文友好提示：

- 未导入总名单就生成周统计或月统计。
- 周统计未选择排班 Word。
- 周统计未选择实际 Word。
- 选择的文件不存在。
- 总名单 Word 未解析到姓名。
- Excel/Word 输出文件被占用。
- 上月 Word 表头被破坏或无法反向解析。
- 标题年份、学期、周数格式不合法。
- 月统计加班补录班次不是正整数。

## 12. 测试与验收需求

自动化测试覆盖范围应包括：

- 总名单解析、去重、全角空格姓名识别。
- 周排班解析、单独成行的星期别名识别、非总名单人员过滤、姓名中包含星期词时不误判标题。
- 总名单配置持久化、损坏配置回退。
- 大四助理名单过滤、去重、持久化、总名单更新后的自动清理。
- 周统计双 Word 口径。
- 放假核减。
- 大四应值固定 1 次及放假优先级。
- 缺班非负。
- 周统计 Excel 列、排序、样式。
- 月统计周 Excel 聚合。
- 上月 Word 多值/缺班结转。
- 上月 Word 空格/斜线单元格按 0。
- 人员流动。
- 月统计 Word 标题替换、表头保留、内置版式、自动编号、斜线注入。
- 月统计加班补录。
- 文件占用异常。

手工验收还应覆盖：

- GUI 中文无乱码。
- 窗口调整后元素不明显错位。
- 对话框提示符合用户操作场景。
- 程序关闭后进程正常退出。

## 13. 当前已知实现状态

从当前工程看，以下能力已经实现：

- 侧边栏多页面 GUI。
- 总名单导入、展示、持久化。
- 大四助理多选、保存、总开关。
- 周统计双 Word 输入。
- 周统计放假核减。
- 周统计 Excel 生成。
- 月统计 Word 生成。
- 上月 Word 结转。
- 月统计加班补录。
- 月度标题手动配置。
- 月统计 Word 内置版式生成。
- 较完整的自动化测试。

## 14. 待确认问题

以下问题无法仅凭当前项目百分百确认，建议由需求方确认：

1. 放假日期是否只需要支持周一到周五？  
   解析器支持周六、周日，但 UI 放假勾选项只有周一到周五。

2. 月统计是否必须严格选择 4 份周 Excel？  
   页面文案说“基于 4 份本月周Excel”，但当前代码允许选择 1 到 4 份。

3. 月统计 Word 的“多值班次/缺班数量”是否允许内部数值为负？  
   当前展示规则是小于等于 0 均显示空白斜线，因此负值不会直接显示；计算中仍使用 `总计 - 应值` 与 `应值 - 总计` 得出正负。

4. 姓名识别是否只允许 2 到 4 个中文字符？  
   当前正则会过滤 1 字名、5 字及以上姓名、含少数民族间隔点或英文字母的名字。

5. `data/config.json` 是否应纳入正式发布包，还是首次运行由用户导入后生成？  
   当前仓库包含一份真实名单配置，发布时可能需要脱敏或清空。

6. 项目是否需要 PyInstaller 打包配置文件？  
   依赖中包含 `pyinstaller`，但仓库中暂未看到 `.spec` 或打包脚本。
