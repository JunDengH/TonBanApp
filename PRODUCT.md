# Product

## Register

product

## Users

资助服务中心负责助理排班、周统计和月统计的工作人员。他们通常在 Windows 桌面环境中集中处理名单、排班文件、实际值班记录和统计报表，需要快速发现错字、漏排、缺班和规则冲突，并生成可继续汇总的标准文件。

## Product Purpose

TonBan 将总名单维护、特殊规则配置、周统计、期末周统计和月统计串成一个可核验的固定流程。成功意味着用户能在不手工数名单、不重复抄表且不破坏源文件的情况下，得到规则一致、来源清晰、可预览修正的 Excel 和 Word 统计结果。

## Brand Personality

可靠、克制、清晰。界面语气直接说明当前状态、阻塞原因和下一步操作，不使用营销语言，不以装饰干扰数据核验。

## Anti-references

- 不做面向展示的营销页或强调视觉噱头的仪表盘。
- 不使用高饱和大面积配色、装饰动画、玻璃拟态或不熟悉的自定义控件。
- 不把错误、警告和成功状态只用颜色表达。
- 不为了减少界面元素而隐藏文件对应关系、规则优先级或输出位置。

## Design Principles

1. 先核验再生成：输入完整性、规则冲突和姓名问题应在写文件前可见。
2. 来源始终明确：每份文件、每个周次和每条警告都能对应到具体统计对象。
3. 熟悉胜过新奇：沿用 Windows 桌面应用和现有 TonBan 的导航、表单、检查栏与对话框模式。
4. 防止半成品：多文件操作先完成整体校验，失败时不留下难以判断的部分结果。
5. 保留人工控制：自动识别减少录入，关键周次、纠错和统计值仍允许用户确认或修正。

## Final-period Constraints

- 默认将 PDF 中两个期末周作为一个统计周期；排班姓名去重后每人应值 2，两份实际 Word 中每次出现计 2。
- 合并期末周统计不使用放假核减，默认只生成一份不含具体周数的 Excel。
- 双周独立导出能力仅作为内部停用分支保留，不在当前界面暴露切换入口。

## Accessibility & Inclusion

使用系统中文字体和标准 CustomTkinter 控件，保持浅色与深色主题可读性。正文与背景对比度以 4.5:1 为最低目标；状态同时使用文字标记和颜色；对话框支持 Escape 关闭，输入控件保留清晰焦点状态。当前项目不声明正式 WCAG 认证。
